# Experiment 01 — Baseline penalty (RQ1)

**Hypothesis:** English-centric tokenizers produce fertility > 5 on Sanskrit, and the
Sanskrit/Hindi parity ratio is > 1.5 on identical FLORES content, because sandhi merges
what Hindi keeps separate.

**Success:** `outputs/01_baseline_penalty/results.json` and `fertility_by_language.pdf`
exist, with fertility, compression and parity for each T0 arm × language × script.

**Expected runtime:** minutes on CPU.
