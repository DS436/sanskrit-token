# Experiment 05 — Language-model training (BPC)

**Hypothesis:** the tokenizer that wins on TPP and MorphScore also reaches a lower
bits-per-character and needs fewer tokens to hit a reference loss.

**Success:** BPC (never perplexity) and tokens-to-reference-loss per arm, mean ± std over
three seeds.

**Expected runtime:** GPU required; TBD.
