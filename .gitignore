.venv/
__pycache__/
*.pyc
*.egg-info/
.mypy_cache/
.ruff_cache/
.pytest_cache/
.superpowers/
.DS_Store

outputs/*
!outputs/.gitkeep

data/raw/*
!data/raw/.gitkeep

data/processed/*
!data/processed/.gitkeep

# Working material about the reader rather than about the research: post drafts and
# sequencing for the LinkedIn series. Deliberately not published; kept on disk only.
docs/outreach/linkedin_plan.md

# Python build artefacts (`uv build` output).
dist/
build/

# Local environment variables and secrets.
.env

# Per-machine Claude Code permissions; not shared.
.claude/settings.local.json
