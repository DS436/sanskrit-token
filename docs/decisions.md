# Decision log

Append-only. Newest entries at the bottom. Format per CLAUDE.md §11.

## 2026-09-03 — Use SLP1 as internal encoding
Why: CharSS 2024 and ByT5-Sanskrit both report SLP1 outperforms Devanagari for neural models; it is lossless and ASCII.
Alternatives: IAST (diacritics complicate tokenizer training), Devanagari (byte-level fragmentation).
Reversible: yes, at cost of re-running tokenizer training.

## 2026-09-03 — Manage environment with uv installed via `python3 -m pip install --user uv`
Why: `uv` was not present on the machine. `pip install --user` bootstraps the tool itself into `~/.local/bin` (already on PATH) without touching the project environment, which stays fully `uv`-managed from `pyproject.toml` + `uv.lock`. Installed version: uv 0.12.9.
Alternatives: the astral install script `curl -LsSf https://astral.sh/uv/install.sh | sh` (equivalent result, extra network trust); Homebrew (adds a package manager dependency).
Reversible: yes, `pip uninstall uv` and reinstall by any other method; no project files depend on how uv was obtained.

## 2026-09-03 — Add `.DS_Store` to `.gitignore`
Why: macOS Finder writes `.DS_Store` into every directory it opens; several were picked up by `git add -A` during the initial scaffold. They carry no project information and would churn every commit.
Alternatives: a global `~/.gitignore_global` (does not travel with the repo, so other contributors would still commit them).
Reversible: yes, delete the line.

## 2026-09-03 — SLP1 roundtrip is guaranteed for Devanagari/IAST only, not for embedded Latin text; no NFC normalisation
Why: SLP1 is itself an ASCII scheme, so `from_slp1` must read Latin letters as phonemes, ASCII digits as Devanagari digits, and `'` `.` `~` `|` as avagraha, danda, candrabindu and Vedic ḻh. A line mixing English with Devanagari therefore cannot roundtrip — `from_slp1` has no way to tell "The" from a run of SLP1 phonemes — and fixture line 10 of `tests/fixtures/devanagari_sample.txt` is kept as a strict `xfail` documenting that boundary. Unicode NFC normalisation was *not* added to `to_slp1`: the eleven pure-Devanagari fixture lines roundtrip without it, and normalising inside `to_slp1` would in fact break identity roundtrip for any decomposed input (the output would come back composed). Callers that need decomposed text handled must normalise on ingest, in `data/`.
Alternatives: wrapping Latin runs in `sanscript` toggle markers (`##`) so they survive the reverse trip — rejected, it injects non-Sanskrit markers into the SLP1 string that tokenizers are trained on, which is the whole point of the encoding. Per CLAUDE.md §2.3 the original script is stored alongside the SLP1 form instead.
Reversible: yes; adding NFC or a toggle-based passthrough is a local change to `encoding.py` plus its tests.

## 2026-09-03 — Load FLORES-200 from the official NLLB tarball, not from a Hugging Face dataset id
Why: `load_flores` tries four sources in order and the first three all failed on this machine, so the download that produced `data/raw/flores/devtest.jsonl` (1012 devtest sentences, `san_Deva`/`hin_Deva`/`eng_Latn`) came from source 4, `https://dl.fbaipublicfiles.com/nllb/flores200_dataset.tar.gz` (sha256 `b8b0b767...944011f6`). Failures, in order: (1) `openlanguagedata/flores_plus` — `DatasetNotFoundError: Dataset 'openlanguagedata/flores_plus' is a gated dataset on the Hub. You must be authenticated to access it.` (no `HF_TOKEN` here); (2) `facebook/flores` — same `DatasetNotFoundError`, now also gated, and independently unusable because `datasets` 5.0.1 has dropped `trust_remote_code` and loading scripts ("trust_remote_code is not supported anymore"); (3) `Muennighoff/flores200` — `RuntimeError: Dataset scripts are no longer supported, but found flores200.py`, the same `datasets` 5.x restriction. The tarball is the upstream release the Hub copies are all derived from, needs no Hub account, and is read with `urllib.request` + `tarfile` from the standard library, so it adds no dependency. It is extracted through `tarfile.extractfile` for only the requested `<split>/<lang>.<split>` members, so no archive path is ever written to disk.
Alternatives: obtaining an `HF_TOKEN` and accepting the `flores_plus` licence (better long-term — `flores_plus` is the maintained release with per-sentence metadata — but it needs a credential this machine does not have, and gating makes the pipeline non-reproducible for anyone without one); pinning `datasets<4` so loading scripts still run (rejected, it would drag the whole environment backwards for one corpus).
Reversible: yes. The four sources are tried in order on every call, so simply setting `HF_TOKEN` and re-running `load_flores` switches the download to `flores_plus` with no code change; the sentences are the same FLORES-200 devtest either way, and only `data/README.md` would need its provenance row updated.

## 2026-09-03 — `T0_llama4` loads `unsloth/Llama-4-Scout-17B-16E-Instruct`, not the gated `meta-llama` id
Why: `meta-llama/Llama-4-Scout-17B-16E-Instruct` is gated and this machine has no `HF_TOKEN`, so `AutoTokenizer.from_pretrained` fails with `GatedRepoError`: "You are trying to access a gated repo. ... 401 Client Error. ... Cannot access gated repo for url https://huggingface.co/meta-llama/Llama-4-Scout-17B-16E-Instruct/resolve/main/config.json." The registry then walks its candidate list and loads `unsloth/Llama-4-Scout-17B-16E-Instruct`, an ungated re-upload of the same Llama-4 Scout release (revision `afd8e498c87bda51c7ea8ec68ea2f7c066e6340b`); vocabulary 201135 ids, matching Llama 4's published ~200k vocabulary, so this is the same tokenizer generation rather than a fallback to Llama-3. `LoadedTokenizer.source_id` carries the id that actually loaded, so `results.json` will always name it. The mirror's tokenizer files could **not** be byte-compared against the gated original, because reading the original is exactly what the gate prevents; if the mirror is ever found to differ, every T0_llama4 number must be re-run.
Alternatives: obtaining an `HF_TOKEN` and accepting Meta's licence (preferred when a credential exists — setting `HF_TOKEN` makes the first candidate succeed with no code change); dropping to `meta-llama/Meta-Llama-3-8B` (also gated) or `unsloth/llama-3-8b` (ungated but a different, older tokenizer, and the arm is named for Llama-4). Both remain in the candidate list below the mirror.
Reversible: yes. The candidate list is tried in order on every load, so setting `HF_TOKEN` switches the arm back to the official id; only the recorded `source_id` changes.

## 2026-09-03 — `T0_gemma3` loads `unsloth/gemma-3-4b-it`, not the gated `google` id
Why: same cause. `google/gemma-3-4b-it` fails with "Cannot access gated repo for url https://huggingface.co/google/gemma-3-4b-it/resolve/main/config.json. Access to model google/gemma-3-4b-it is restricted. You must have access to it and be authenticated to access it." The registry falls through to `unsloth/gemma-3-4b-it` (revision `bf46152c47f5dd20b896357cb51abc4c03b8ee8c`), an ungated re-upload of the same Gemma-3 release; vocabulary 262145 ids, matching Gemma 3's published 262k vocabulary. `transformers` 5.x instantiates it as the SentencePiece-backed `GemmaTokenizer` rather than a fast backend; that is the library's own default for this repo and is left alone, as the T0 arms are meant to report *existing practice* (CLAUDE.md §2.5). Same caveat as for T0_llama4: the mirror cannot be byte-compared against the gated original.
Alternatives: `HF_TOKEN` plus the Gemma licence; `google/gemma-2-2b` / `unsloth/gemma-2-2b`, the previous generation, kept as the last two candidates.
Reversible: yes, identically — set `HF_TOKEN` and the first candidate wins.

## 2026-09-03 — Report `len(tokenizer)` as an arm's `vocab_size`, not `tokenizer.vocab_size`
Why: the two disagree wherever a repo adds special tokens on top of the base vocabulary — Llama-4 Scout reports `vocab_size` 200000 while its ids run to 201134, Gemma-3 reports 262144 against 262145. The id space is what an embedding matrix must cover and what makes vocabulary sizes comparable across arms, so it is the number carried into `results.json`. For `T0_o200k` the equivalent is tiktoken's `Encoding.n_vocab` (200019).
Alternatives: `tokenizer.vocab_size` (understates every repo with added tokens); reporting both (extra field with no consumer yet — add it if a later experiment needs the split).
Reversible: yes, one line in `_hf_vocab_size`; no stored artefact depends on it until Experiment 01 writes its `results.json`.

## 2026-09-03 — Experiment 01 summarises metric distributions as mean + population std, and drops the per-item lists from `results.json`
Why: `fertility`, `compression` and `parity` each return a per-item distribution (`per_word`, `per_text`, `per_pair`) alongside `value`. Written verbatim, three arms × three languages × two script variants × 1012 sentences (and ~25k words per language) would put on the order of a million floats into `results.json`, making it unreadable and unusable in diffs. Each result is therefore reduced by `summarise_metric` to `{value, n, unit, distribution, mean, std}`, where `distribution` names the key that was summarised, `mean` is `statistics.fmean` over it and `std` is `statistics.pstdev` (population std, matching `numpy.std`'s default; `0.0` for a single item). `mean` is kept even though it duplicates `value` for fertility, because for compression and parity the metrics pool numerator and denominator over the whole corpus, so the mean of the per-item ratios is a genuinely different number and is the one that pairs with `std`.
Alternatives: writing the full distributions (unreadable, and git-hostile); writing them to a separate `distributions.npz` per experiment (deferred — no consumer needs them yet; add it when a figure needs a histogram); reporting sample std with `ddof=1` (rejected for consistency with `numpy.std`, which later experiments will use).
Reversible: yes. `summarise_metric` is one function in `experiments/01_baseline_penalty/run.py`; re-running the experiment regenerates `results.json` in ~13 s.

## 2026-09-03 — Report SLP1 roundtrip failures in three buckets, not two, and never abort on them
Why: the plan asked for a roundtrip assertion over every `san_Deva` sentence, split into failures that contain Latin letters (expected: SLP1 is ASCII, so `from_slp1` cannot tell an English word from a run of phonemes) and "pure Devanagari" failures that would indicate a real transliteration gap. On FLORES devtest that cut is not tight enough: 317/1012 sentences fail, 92 with Latin letters and 225 without — but 136 of those 225 merely contain ASCII digits ("4", "2016"), which `from_slp1` renders as Devanagari digits, and of the 89 that contain no ASCII letter or digit at all, nearly all are ASCII `.` or `|` used as danda (SLP1 reads `.` as danda and `|` as Vedic ḻh), with a single genuine nukta case at index 28 (`प़ृ` → `ऋ`). Reporting only "225 pure-Devanagari failures" would have badly overstated the problem. `roundtrip_report` therefore keeps `failures_with_latin` and `failures_pure_devanagari` (the latter meaning "no Latin *letter*", as specified) and adds `failures_no_ascii_alnum` plus `examples_no_ascii_alnum`, which is the bucket actually worth reading; the WARNING log prints all three. The run does not abort, because every metric is computed on the original script *and* on SLP1 and both are reported, and the corpus keeps the original script alongside (CLAUDE.md §2.3), so nothing downstream depends on the reverse direction being lossless.
Alternatives: asserting and aborting (would make Experiment 01 unrunnable on the real corpus for a reason that does not affect any of its numbers); normalising ASCII digits and `|` to their Devanagari equivalents on ingest (rejected here — it would silently edit the corpus every later experiment is measured on; if it is ever wanted it belongs in `data/`, with its own decision entry).
Reversible: yes; the buckets are computed in one function and no stored artefact other than `results.json` depends on them.

## 2026-09-03 — Experiment 01 config paths resolve against the repository root, not the working directory
Why: `config.yaml` holds `flores_jsonl: data/raw/flores/devtest.jsonl` and `output_dir: outputs/01_baseline_penalty`. Resolved against the CWD, `uv run python experiments/01_baseline_penalty/run.py` would read and write different places depending on where it was invoked from, and would silently re-download FLORES into a stray `data/` directory. `repo_root()` derives the root from `__file__` (the parent of `experiments/`) and `resolve_path` joins relative config paths onto it, leaving absolute paths untouched.
Alternatives: requiring the CWD to be the repo root (an undocumented precondition that fails quietly); making every config path absolute (not portable across machines, and would be committed).
Reversible: yes, two small functions.

## 2026-09-03 — Report the Hindi SLP1 variant as approximate, and record SLP1 coverage in `results.json`
Why: Experiment 01 transliterates every Devanagari language to SLP1 so that `hin_Deva` and `san_Deva` can be measured on the same footing, but SLP1 encodes the *Sanskrit* phoneme inventory and Hindi is not inside it. Two failure modes, both silent: nukta consonants (`क़ ज़ ड़ ढ़ फ़`, and the precomposed U+0958–U+095F letters) have no SLP1 phoneme and `sanscript` emits the sign as a literal ASCII `0` (`क़` → `k0a`), and signs outside the scheme — chiefly candra-o and candra-e, `ॉ ऑ ॅ` — pass through unconverted, so a string that is supposed to be ASCII still holds raw three-byte Devanagari (`डॉक्टर` → `qaॉkwara`). On FLORES-200 devtest that is 513/1012 Hindi sentences with a nukta and 256/1012 Hindi SLP1 strings still non-ASCII; `san_Deva` is hit too, at 40/1012 and 100/1012, mostly through proper nouns. Both effects inflate the SLP1 byte count and change how a tokenizer segments the string, so the `hin_Deva` SLP1 fertility and compression figures are indicative, not measurements. Rather than delete or silently keep them, `run.py` gained a pure `slp1_coverage(texts_original, texts_slp1)` that counts both failure modes per language, writes them to `results.json` under `slp1_coverage` (next to `roundtrip`), and logs them at WARNING; the experiment README labels the `hin_Deva (SLP1)` column approximate and carries the counts. No number changed: `metrics` and `parity` in `results.json` are byte-identical to the previous run.
Alternatives: dropping the `hin_Deva` SLP1 column entirely (rejected — it is a useful sanity check that Devanagari byte inflation, not Sanskrit, drives the original-script compression gap, and deleting it would hide the finding rather than qualify it); transliterating Hindi with a Hindi-aware scheme such as ISO 15919 or WX (rejected for now — it would put the two languages in different encodings, which defeats the point of the column, and CLAUDE.md §2.3 fixes SLP1 as the internal encoding; revisit if a later experiment needs Hindi text as more than a control); normalising nukta away on ingest (rejected — it silently edits the corpus every later experiment is measured on, and if ever wanted belongs in `data/` with its own entry).
Reversible: yes. `slp1_coverage` is one pure function plus one key in `results.json`; the column can be dropped, or re-derived under a different scheme, by re-running the experiment (~13 s warm).

## 2026-09-03 — Metric summaries report `mean`/`std` as `null` when there is no distribution to summarise
Why: `summarise_metric` wrote `0.0` for `mean` and `std` whenever a metric attached no per-item distribution, or attached an empty one. `0.0` is a perfectly plausible value for a fertility, a bytes-per-token or a parity ratio, so an empty or missing distribution was indistinguishable in `results.json` from a genuine measurement of zero — the failure mode CLAUDE.md §9 exists to prevent, since `results.json` is where every number lives. They are now `None` (JSON `null`), and `summarise_metric` returns a `MetricSummary` TypedDict declaring `mean: float | None` / `std: float | None` so `mypy --strict` enforces that consumers handle it. A single-item distribution still reports `std` `0.0`, which is a real measurement. No stored number changed: every metric in the current `results.json` attaches a non-empty distribution.
Alternatives: omitting the keys entirely when there is nothing to report (rejected — it makes every consumer branch on key presence, and a fixed schema is easier to diff across runs); keeping `0.0` and relying on `n` to disambiguate (rejected — `n` is the metric's own denominator, not the distribution's length, so the two can disagree).
Reversible: yes, one function and its TypedDict in `experiments/01_baseline_penalty/run.py`.

## 2026-09-03 — Tokenizer loading and metric inputs fail loudly instead of being swallowed
Why: three places treated a defect as an expected condition. (1) `registry._load_hf_arm` caught bare `Exception` per candidate, so a `TypeError` from a `transformers` signature change or a bug in `_hf_vocab_size` would be logged as "could not load", retried against three more model ids, and finally reported as "no candidate tokenizer could be loaded" — pointing every future debugging session at Hugging Face rather than at this repository. It now catches `OSError`, which is exactly the "repository not available to you" family: `huggingface_hub`'s `GatedRepoError` and `RepositoryNotFoundError` derive from `HfHubHTTPError` → `requests.HTTPError` → `OSError`, as do network failures, and `transformers` re-raises an unresolvable repo as `OSError("Can't load tokenizer for ...")`. Verified against the existing tests, which raise `OSError`, and against the live gated-repo path in the Experiment 01 re-run. Known gap, documented in the docstring: `huggingface_hub.errors.EntryNotFoundError` is *not* an `OSError` in `huggingface_hub` 1.x; `transformers` currently converts that case, and if it stops doing so the arm raises instead of falling through — the safe direction, since a mirror missing its tokenizer files is not a licence gate. (2) `tests/test_registry.py::_skip_if_tiktoken_is_offline` caught bare `Exception`, so any bug in `_load_tiktoken_arm` would turn into a green skip; narrowed to `OSError` likewise. (3) `str` is itself a `Sequence[str]`, so `fertility(tok, "rAmaH gacCati")` type-checked, ran, and returned a number computed one *character* at a time — a plausible-looking wrong answer rather than an error. `tokenizers/base.require_texts` now rejects a bare `str` with `TypeError`, and `fertility`, `compression` and `parity` (both sides) call it.
Alternatives: catching `Exception` but re-raising anything that is not a hub error (same effect, more code); leaving the metrics unguarded and relying on review (rejected — the wrong number is publishable-looking, which is the worst kind); accepting a bare `str` as a one-text corpus (rejected — it silently guesses at intent, and `[text]` is one character longer to write).
Reversible: yes, all three are single-line changes with tests pinning them.

## 2026-09-03 — Undefined per-item ratios are NaN, never 0.0
Why: compression `per_text` and parity/TPP `per_pair` can be undefined (zero tokens on one side). A 0.0 sentinel is indistinguishable from a measured zero and silently drags any mean or bootstrap CI down. NaN is visible, `numpy` handles it with nan-aware reductions, and JSON round-trips it. Every metric also records `n_undefined`.
Alternatives: 0.0 sentinel (rejected: silent contamination); dropping the item (rejected: hides that it existed).
Reversible: yes; only `metrics/` and `metrics/summary.py` change.

## 2026-09-03 — TPP is parity plus a paired bootstrap over a shared ratio core
Why: CLAUDE.md §7 defines both parity and TPP as sum(tokens_a)/sum(tokens_b) over aligned pairs; the only differences are the corpus (FLORES vs. translation corpora), the unit, and the bootstrap CI TPP requires. One core (`metrics/_ratio.py`) prevents two copies drifting. The bootstrap resamples pair indices with replacement and recomputes the ratio of sums (not the mean of per-pair ratios), 1000 draws, seed from config, 95% percentile CI.
Alternatives: independent implementations (rejected: drift); mean of per-pair ratios (rejected: dominated by short sentences).
Reversible: yes.

## 2026-09-03 — Token spans for MorphScore deferred to Experiment 04; fast tokenizers required there
Why: the `Tokenizer` Protocol only exposes `encode -> list[int]`, which is enough for every count-based metric in Experiments 01–03. MorphScore needs character spans per token. When Experiment 04 starts, add a `TokenizerWithSpans` Protocol (`spans(text) -> list[tuple[int, int]]`) satisfied by HF fast tokenizers via `return_offsets_mapping` and by trained `tokenizers` models natively; T0_gemma3 currently loads as a slow SentencePiece tokenizer and must be reloaded with `use_fast=True` (or spans reconstructed from pieces) at that point.
Alternatives: add spans now (rejected: YAGNI for Exp02).
Reversible: yes.

## 2026-09-03 — MorphScore reports F1 as `value`, precision and recall as extras
Why: `MetricResult` has one `value`. For Experiment 04, `value` = F1 of token boundaries vs. gold morpheme boundaries, with `precision` and `recall` as additional keys so over- vs. under-segmentation stays visible, per Arnett & Bergen.
Alternatives: two separate results (rejected: doubles every table).
Reversible: yes.

## 2026-09-03 — Add `T0_gpt2` as an older-generation English-centric arm
Why: all three current T0 arms have ≥200k vocabularies, which is the likely reason fertility on Sanskrit came in at 3–4 rather than the pre-registered >5 (calibrated on 32k–50k-vocab tokenizers). GPT-2 (50,257, ungated, `openai-community/gpt2`) lets "existing practice" span tokenizer generations. It is reported alongside the others as existing practice, never as a controlled comparison.
Alternatives: Llama-2 32k (gated, no token); none (rejected: leaves the negative result under-explained).
Reversible: yes; drop the arm from configs.

## 2026-09-03 — Provisional T1/T2 tokenizers trained on the Sanskrit sides of Sāmayik and Itihāsa training splits
Why: RQ2's sign-flip test needs a Sanskrit-native tokenizer, and the monolingual corpus (DCS, GRETIL, Wikipedia; milestone M1) is not assembled yet. The Sanskrit sides of the two parallel training splits (~120k sentences) are available now, cleanly separated from every evaluation split, and enough for a 32k/64k subword vocabulary. Training text is SLP1 (internal encoding); pre-tokenization is `Metaspace` so no token crosses whitespace; the exclusion-hash assertion runs before training. These arms are labelled provisional in every table and will be retrained on the monolingual corpus when M1 completes.
Alternatives: wait for M1 (rejected: blocks the headline result); train on Devanagari (deferred to ablation A1).
Reversible: yes; retraining is one command.

## 2026-09-03 — Experiment 02 corpora and pivots
Why: TPP is computed on Sāmayik test and the Mann Ki Baat out-of-domain test (prose, primary), Itihāsa test (verse, secondary), and FLORES devtest (Wikipedia, tertiary), in that order in every table and figure. English is tokenized with o200k (primary pivot) and Llama-4 (secondary). The Hindi pivot uses FLORES only; SAHAAYAK (1.5M Sa–Hi) is deferred until the Hindi side is needed at scale. The exclusion list is built from every evaluation split named here plus FLORES devtest and committed to git (sha256 of the SLP1 form, derived from public data).
Alternatives: Itihāsa first (rejected: meter confound, CLAUDE.md §2.7); SAHAAYAK now (rejected: size, not needed for RQ2).
Reversible: yes.

## 2026-09-03 — `T3_indicsuper` unavailable: not publicly released as of 2026-09-03
Why: none of the three plausible repository ids resolves. Verified twice, independently: `huggingface_hub.list_repo_files` returns `RepositoryNotFoundError` (401, "Repository Not Found") for `krutrim-ai-labs/IndicSuperTokenizer`, `ai4bharat/IndicSuperTokenizer` and `ai4bharat/indic-super-tokenizer`, and a live `load_tokenizer("T3_indicsuper")` run (`SANSKRIT_TOK_NETWORK_TESTS=1`) walks all three via `AutoTokenizer.from_pretrained` and gets the same "not a valid model identifier" error from each before raising `TokenizerUnavailable`. This is a real registry entry, not a stub: the moment any of the three ids is published, `load_tokenizer("T3_indicsuper")` starts loading it with no code change, and `results.json` will record which one under `tokenizer_sources`. Experiments must catch `TokenizerUnavailable` for this arm and record it under `unavailable_arms` rather than aborting the run.
Alternatives: dropping the arm from the registry until it exists (rejected — CLAUDE.md §6 names it explicitly, and re-adding it later would be indistinguishable from a fresh addition rather than a name the project always intended to measure); guessing at a fourth id (rejected — no fourth candidate is documented anywhere; adding one on speculation risks silently measuring the wrong tokenizer under this arm's name).
Reversible: yes — this is a standing candidate list, not a permanent judgement; re-run the network-gated test whenever the arm might have shipped.

## 2026-09-03 — `T0_gpt2` and three of four `T3` arms load their first (only) candidate; no substitutions
Why: live loads (`SANSKRIT_TOK_NETWORK_TESTS=1`, no `HF_TOKEN` on this machine) of `T0_gpt2`, `T3_sarvam`, `T3_sutra` and `T3_brahmic131k` all succeed on the first and only candidate in each arm's list, via plain `AutoTokenizer.from_pretrained` — none is gated, so no fallback and no substitution to record. `T3_brahmic131k` in particular resolves at tier 1 of `_load_brahmic131k_arm`'s three adapter tiers (`AutoTokenizer`, not the `tokenizers.Tokenizer.from_pretrained` or `.tiktoken` fallback), because `theschoolofai/BrahmicTokenizer-131K` ships a plain `tokenizer.json` + `tokenizer_config.json` that `AutoTokenizer` resolves directly; the other two tiers exist for a future repo revision that drops the `transformers`-compatible config, and are exercised by no live arm today. Measured: `T0_gpt2` 50,257 ids (`openai-community/gpt2`); `T3_sarvam` 68,096 ids (`sarvamai/sarvam-1`); `T3_sutra` 256,061 ids (`TWO/sutra-mlt256-v2`); `T3_brahmic131k` 131,072 ids (`theschoolofai/BrahmicTokenizer-131K`).
Alternatives: n/a — recorded because CLAUDE.md §11 asks every substitution to be logged, and this entry establishes that these four are the unsubstituted baseline against which any future fallback (e.g. if `sarvamai/sarvam-1` is ever gated) would be a documented change.
Reversible: n/a.

## 2026-09-03 — Itihāsa and Sāmayik have no LICENSE file at their pinned commits; corrected `data/README.md`
Why: `data/README.md`'s placeholder row already listed Itihāsa as Apache-2.0, but neither `rahular/itihasa` at `37df077a80c83dbba1598afbdb079674b9bf6daa` nor `ayushbits/Saamayik` at `f87d54903e9f8540bda63efeb2c203e368195523` carries a LICENSE file — verified by fetching each repository's full git tree at the pinned commit via the GitHub API and finding no `LICENSE`/`LICENSE.md`/`LICENSE.txt` entry in either. Neither repo's GitHub API response advertises an SPDX license either. Rather than keep the pre-filled (and apparently guessed) Apache-2.0 claim, both rows now read "unspecified (repo has no LICENSE file; dataset described in ... )", citing the paper that describes each dataset (WAT 2021 for Itihāsa, LREC-COLING 2024 for Sāmayik) instead of a license this project cannot substantiate.
Alternatives: keeping the placeholder Apache-2.0 for Itihāsa (rejected — CLAUDE.md §5 requires recording the license actually found, and repeating an unverified claim in a committed provenance table is worse than an honest "unspecified"); inferring a license from the dataset's paper (rejected — a paper describing a dataset is not a license grant, and neither paper states one for the code/data repository).
Reversible: yes — if either repository later adds a LICENSE file, update the row and re-verify via the same API-tree check.

## 2026-09-03 — Exclusion-list dedup collapses 274 of 27,761 evaluation sentences, mostly Sāmayik `test_ood`
Why: `build_exclusion_list` hashes the SLP1 form of every sentence across `flores_devtest`, `samayik_dev`, `samayik_test`, `samayik_test_ood`, `itihasa_dev` and `itihasa_test` and keeps only unique hashes, so the committed `data/exclusion_hashes.txt` has 27,487 lines of hashes against 27,761 input sentences (the header's `sources:` counts are the honest per-source input counts, taken before dedup). Checked where the 274 collapsed sentences come from: 264 are duplicate sentences *within* a single source (217 of those inside `samayik_test_ood` alone — Mann Ki Baat is address transcripts with recurring boilerplate phrases and salutations, so repeated sentences are expected content, not a data bug) and 10 are the same sentence appearing in two different sources. None of this changes what the loaders return: `load_samayik`/`load_itihasa` keep every duplicate line exactly as the source repository has it (CLAUDE.md §8 loaders do I/O, not corpus editing); dedup is scoped to the exclusion-hash *set*, where it belongs, since the point of that file is "was this sentence seen," not "how many times."
Alternatives: deduplicating within the corpus loaders themselves (rejected — silently drops real content the authors intended to keep, and would change `len(corpus)` away from the published split sizes this task verified: Sāmayik train 43,493 / dev 2,416 / test 2,417 / test_ood 4,047, Itihāsa train 75,161 / dev 6,148 / test 11,721); reporting unique-sentence counts instead of raw input counts in the exclusion file's `sources:` header (rejected — the header is documenting what was fed in, and the hash count on the following lines already gives the unique total).
Reversible: yes — both are read-only observations; nothing about the loaders or the hash format changes if this is revisited.

## 2026-09-03 — T1/T2 training run: filtered 218 leaked sentences before the corpus build; no vocab shortfall
Why: the first real run of `experiments/02_tpp_parallel/train_tokenizers.py` hit `build_training_corpus`'s `assert_not_excluded` and raised `LeakageError` — 178 of Sāmayik `train`'s 43,493 sentences and 40 of Itihāsa `train`'s 75,161 collide with `data/exclusion_hashes.txt`. Inspected every offender: the Sāmayik collisions are formulaic course-material lines that recur verbatim across its own splits (e.g. "पाठान्ता: प्रश्ना:", "end-of-lesson questions"; also UI strings like "Selector tool उपरि नुदन्तु"), and the Itihāsa collisions are famous, oft-quoted ślokas that recur verbatim across the epic (e.g. the "नारायणं नमस्कृत्य..." invocation, which opens multiple Mahābhārata parvans and appears at both index 21652 and 23659 of `train` itself). Both are genuine upstream artifacts of how these corpora were split, not a bug in the loaders or the hashing. `train_tokenizers.py` now calls a new `filter_leaked_sentences` step (not part of `corpus.py`) between loading each source and calling `build_training_corpus`: it drops every sentence whose Devanagari hash is in the exclusion set and logs a WARNING with the per-source count, so `build_training_corpus`'s own assertion stays in place as a safety net and is expected to pass trivially afterwards. `corpus.py`'s `build_training_corpus` itself is unchanged from the brief's spec (assert-then-raise on its input) — filtering happens one layer up, in the script that has the freedom to decide what to do about a real leak, rather than in the reusable, side-effect-free corpus builder.
Actual run, after filtering (218 sentences dropped: 178 + 40): corpus manifest `n_in={"samayik_train": 43315, "itihasa_train": 75121}`, `n_out=117720`, `n_dedup_removed=716` (exact duplicates across the two sources), `sha256=7d36ba6c...a3faa7`. All four arms reached their requested vocabulary size exactly — `T1_bpe_raw_32k` 32,000 (4.1s), `T1_bpe_raw_64k` 64,000 (5.0s), `T2_unigram_raw_32k` 32,000 (23.5s), `T2_unigram_raw_64k` 64,000 (20.4s) — so the brief's anticipated "Unigram vocab cannot be reached" fallback was not needed at ~117.7k training sentences.
Alternatives: aborting the run and hand-editing the Sāmayik/Itihāsa raw files to remove the duplicate lines (rejected — mutates cached, gitignored source data the loaders are supposed to read verbatim, and the duplication is a property of the corpus, not an error to "fix" in place); silently deduping via `build_training_corpus`'s existing exact-dedup step instead of a separate filter (rejected — dedup and leakage are different conditions: a sentence can leak without being an exact duplicate of anything else in the training sources, e.g. if it appears only once in `train` and once in `test`, so conflating the two would have hidden the leak-count signal that flagged this).
Reversible: yes — `filter_leaked_sentences` is a pure function over the loaded sentences; removing it restores the original assert-and-abort behaviour, at the cost of `train_tokenizers.py` failing again on this corpus.

## 2026-09-03 — `UnigramTrainer` is not bit-for-bit deterministic; `train_bpe` is
Why: a code-review pass asked to confirm the four trained `tokenizer.json` files were byte-identical to an earlier run of `experiments/02_tpp_parallel/train_tokenizers.py` on the same corpus. `T1_bpe_raw_32k` and `T1_bpe_raw_64k` were (sha256 matched exactly); `T2_unigram_raw_32k` and `T2_unigram_raw_64k` were not. Reproduced and isolated in-process, outside this repo's code entirely: calling `Tokenizer(models.Unigram())` + `UnigramTrainer(vocab_size=300, ...)` twice in a row on the *same* fixture file, with no intervening state change, produces two different serialized tokenizers. Diffed the two: the vocabulary token *set* is identical (300/300 tokens match) and `token->id` mapping differs only for a handful of near-tied entries; the underlying per-piece EM scores differ by a few units in the last floating-point digit (e.g. `-4.719180951391835` vs. `-4.719180951391834`), consistent with floating-point summation order varying across threads inside the Rust EM implementation (`tokenizers`' `UnigramTrainer` runs its E-step in parallel via `rayon` and exposes no seed to pin thread scheduling). `train_bpe.train_bpe`'s `BpeTrainer` has no such step — confirmed deterministic across three repeated in-process runs on the same fixture — so this is specific to Unigram, not to this repo's wrapper or to file I/O. `train_unigram.py`'s docstring is corrected to state this plainly rather than imply full determinism from the (true, but narrower) fact that `UnigramTrainer` has no seed *parameter*.
Consequence for reproducibility (CLAUDE.md §8): the *vocabulary size* and *content* of a T2 arm are reproducible from `data/processed/tok_train_slp1.txt` + `tokenizers.yaml`, and `results.json` still records `git_commit`/`config`/`manifest`/`train_seconds` faithfully for the run that produced the file actually on disk — but a fresh `train_unigram` run is not guaranteed to reproduce a previously-committed `tokenizer.json` byte-for-byte, only a tokenizer with the same vocabulary. Any downstream comparison across two Unigram-training runs (e.g. re-running exp02's TPP figure after retraining T2 on the monolingual corpus at M1) should tokenize fresh text with the specific `tokenizer.json` on disk at the time, not assume it can be regenerated identically from the recipe alone.
Alternatives: trying to force determinism (e.g. `RAYON_NUM_THREADS=1`) before every Unigram training call (rejected for this task — unverified whether it actually pins float summation order to a canonical one across machines/library versions, adds a global environment mutation to a library call, and the *content* is what CLAUDE.md's reproducibility guarantee is actually for; worth revisiting if a future task needs literal byte-reproducibility); silently doing nothing (rejected — the review explicitly asked to confirm-or-explain, and an unexplained sha256 mismatch on a "provisional but real" trained artifact is exactly the kind of thing this log exists to catch, CLAUDE.md §11).
Reversible: yes — this is an observation about upstream library behaviour, not a change to this repo's training settings; the corrected docstring can be reverted without functional effect if a future `tokenizers` release changes this behaviour (re-verify before assuming so).

## 2026-09-03 — `mypy src experiments` cannot type-check two experiments' `run.py` files in one invocation; verified per-experiment instead
Why: adding `experiments/02_tpp_parallel/run.py` alongside the existing `experiments/01_baseline_penalty/run.py` makes `uv run mypy src experiments` fail with `Duplicate module named "run"`, not a defect in either file. Every `experiments/NN_name/` directory (CLAUDE.md §3) has no `__init__.py` (each experiment is a standalone script, not a package — the existing convention `tests/test_exp01.py` already documents by loading `run.py` via `importlib` rather than importing it), so mypy assigns each freestanding `run.py` a module name of just `run`, scoped to its own directory; two such files anywhere under the same invocation collide. Tried the three resolutions mypy's own error message suggests: `--exclude` (works but permanently drops one file from type coverage), `__init__.py` "somewhere" (mypy itself refuses: `NN_baseline_penalty contains __init__.py but is not a valid Python package name`, because every experiment directory starts with a digit, which is not a valid Python identifier component — CLAUDE.md §3's own naming scheme rules this out for every current and future experiment), and `--explicit-package-bases`/`MYPYPATH` (verified empirically, with `-v` tracing mypy's own `BuildSource` resolution, that this only changes module-name computation for files discovered *underneath* an existing `__init__.py` chain or an implicit-namespace directory tree; a fully freestanding script with zero `__init__.py` anywhere above it is still named by its own basename regardless of `MYPYPATH`, so this does not help either). This is a structural property of the digit-prefixed `NN_name/` layout combined with every experiment reusing the filename `run.py`, not something introduced by this task or fixable by editing `experiments/02_tpp_parallel/` alone — it will recur identically when `03_sandhi_split/run.py` etc. are added. Verified instead with `uv run mypy src experiments/02_tpp_parallel` (28 source files, clean) and, as a regression check, `uv run mypy src experiments/01_baseline_penalty` (26 source files, clean, unchanged) — both experiments are fully `mypy --strict` clean individually; only the *combined, whole-tree* invocation is structurally impossible while the layout stands.
Alternatives: adding `exclude` regex to `[tool.mypy]` in `pyproject.toml` to drop one experiment's `run.py` from combined checks (rejected — silently removes strict-mode coverage from a file that is otherwise clean, and would need a growing list of exclusions as more experiments are added); renaming `run.py` per experiment, e.g. `run01.py`/`run02.py` (rejected — breaks the established one-`run.py`-per-experiment convention CLAUDE.md §3 and Experiment 01 already established, for a lint-tool limitation rather than a real ambiguity); adding `__init__.py` (rejected — mypy refuses it outright for a digit-leading directory name, see above); leaving `uv run mypy src experiments` broken and undocumented (rejected — the next experiment's author would waste time re-diagnosing the same collision).
Reversible: yes — this is an observation about mypy's module-naming rules, not a code or config change; if a future mypy release adds a way to type-check same-named freestanding scripts together, or if the experiment directories are ever restructured as proper packages (e.g. renamed without a leading digit, or nested one level under an `__init__.py`-bearing nonnumeric parent), the combined invocation can be revisited.

## 2026-09-03 — Experiment 02 verdict reframed: consistent with H2, not yet separable from domain fit
Why: the TPP sign flip appears only on Sāmayik test and Itihāsa test, which are in-domain for the provisional T1/T2 tokenizers (trained on those corpora's training splits), and does not appear on Sāmayik test_ood or FLORES, which are out-of-domain; the English pivot (o200k) is general-domain everywhere. Domain fit therefore predicts the observed pattern as well as H2 does. The README verdict must say so and must not read as confirmation until a matched English control (next entry) or the M1 monolingual retrain separates the two.
Alternatives: keep "confirmed, narrowly" (rejected: conflates Sanskrit-native with in-domain, the token-level analogue of the words/tokens conflation CLAUDE.md §1 forbids).
Reversible: yes; wording only.

## 2026-09-03 — Add a matched English control family E1 for TPP
Why: every trained Sanskrit arm (32k/64k) is currently compared against a 200k-vocab English tokenizer, so the TPP ratio mixes "language" with "vocabulary size and training domain". Training `E1_bpe_{32k,64k}` and `E1_unigram_{32k,64k}` on the English side of the same Sāmayik+Itihāsa training splits, with the same trainer settings and the same exclusion assertion, gives the controlled comparison: same algorithm, same vocab, same domain on both sides. TPP T1/T2 vs E1 becomes the controlled headline; TPP vs o200k/Llama-4 is reported as deployed-practice TPP. E1 is a new family outside CLAUDE.md §6's arm table and is added to it.
Alternatives: none (rejected: leaves the headline uncontrolled); English trained on a different corpus (rejected: reintroduces the domain mismatch).
Reversible: yes; drop the arms from configs.

## 2026-09-03 — Record `git_dirty` and file hashes in every results.json
Why: Experiment 02's results.json recorded the parent commit while containing a key introduced by uncommitted code (run-then-commit). Every results writer now records `git_dirty: bool` (from `git status --porcelain`) and, for file-backed tokenizer arms, the sha256 of `tokenizer.json`, so artifacts are verifiable even though Unigram training is not bit-reproducible and `outputs/` is gitignored.
Alternatives: commit before running (rejected: results must reflect the code that ran, and the README needs the numbers).
Reversible: yes.

## 2026-09-03 — `fertility` joins the undefined-is-`nan` contract; `tpp` validates `ci`
Why: `compression` and `parity` already return `nan` for an undefined ratio and reserve `0.0` for a real measurement (`summary.py`'s own docstring turns on that distinction), but `fertility` returned `0.0` both for genuinely empty input and for non-empty text holding no words — indistinguishable in `results.json` from a tokenizer that measured a real zero. It now returns `nan` when `texts` is non-empty but yields no words, keeps `0.0`/`n=0` for `texts == []` (nothing was measured at all), and adds `n_undefined` = how many words encode to zero tokens, a diagnostic for a vocabulary that silently deletes a word and thereby makes `value` optimistic. `summarise_metric` drops `n_undefined`, so no `results.json` shape changes; verified by re-running Experiments 01 and 02 and diffing every metric, parity and TPP value against the previous `results.json` (0 differences), and by re-computing all 20 of Experiment 01's fertility measurements directly: `n_undefined == 0` throughout, i.e. no FLORES word in any of the three languages, either script, under any T0 arm, encodes to zero tokens. Separately, `tpp` now raises `ValueError` unless `0 < ci < 1`: `ci` is a confidence level (`0.95`), and every plausible mistyping produces an interval that still looks like a measurement — `95` pushes the percentiles outside `[0, 100]`, which `numpy` clamps silently to the extremes, and `0.05` yields a needle-thin CI that reads as high precision.
Alternatives: leaving fertility's `0.0` (rejected — CLAUDE.md §7 metric contract, and the inconsistency is exactly the kind that turns into a wrong claim once a corpus with an empty split appears); clamping or warning on a bad `ci` instead of raising (rejected — a silently wrong CI is the failure mode this guards against).
Reversible: yes; both are local to their metric module.

## 2026-09-03 — Git provenance lives in `src/sanskrit_tok/provenance.py`, not in each `run.py`
Why: `git_commit` had been copy-pasted into `experiments/01_baseline_penalty/run.py` and both Experiment 02 scripts, and `git_dirty` (previous entry) would have made that three copies of two functions. All three now import `git_commit`/`git_dirty` from one module, tested against a real throwaway git repository built in `tmp_path` rather than a mocked `subprocess` — the thing under test is the subprocess call and its output parsing, so mocking it would prove nothing. `experiments/02_tpp_parallel/run.py` also records `sha256` of the `tokenizer.json` for every file-backed (T1/T2) arm under `tokenizer_sources`, which is the artifact hash the previous entry asked for.
Alternatives: a fourth copy in each script (rejected — CLAUDE.md §8, and the copies had already drifted in docstring detail); putting the helpers in a shared `experiments/` module (rejected — `experiments/NN_name/` directories are not importable packages, docs/decisions.md "mypy cannot type-check two experiments' `run.py`…").
Reversible: yes.

## 2026-09-03 — E1 matched control: the Sāmayik TPP flip does not survive it; Itihāsa's does
Why: the four `E1_*` arms are trained (same trainers, same settings, same 32k/64k sizes) on the English side of the same Sāmayik+Itihāsa training splits whose Sanskrit side trained T1/T2, so at each corpus both sides of the TPP ratio have the same domain fit — in-domain on Sāmayik test and Itihāsa test, out-of-domain on `test_ood` and FLORES. Result (`outputs/02_tpp_parallel/results.json`, `tpp_controlled`): Sāmayik test 1.027–1.148, Sāmayik test_ood 1.067–1.161, FLORES 1.138–1.219 — all sixteen of those CIs entirely **above** 1.0 — and Itihāsa test 0.607–0.663, all four CIs entirely **below**. The headline flip this experiment previously reported (`T1_bpe_raw_64k` 0.908 [0.896, 0.921] on Sāmayik test against `T0_o200k`) becomes 1.027 [1.014, 1.040] against `E1_bpe_64k`, with the Sanskrit side untouched: the English side moved, because the control arm is in-domain and so cheaper on English (34,801 tokens vs o200k's 39,339 on Sāmayik test, 11.5% fewer; 376,705 vs 484,898 on Itihāsa test, 22.3% fewer). Per the previous entry's own criterion ("an in-domain flip that survives against E1 is evidence for H2; one that vanishes is domain fit"), the prose flip is domain fit. The README verdict is rewritten accordingly: **not supported on prose**; the one surviving flip is on verse, where meter (CLAUDE.md §2.7) and a 19th-century verse translation in the denominator are both confounds. The deployed-practice numbers (every arm vs `T0_o200k`/`T0_llama4`) are byte-identical to the pre-E1 run — verified by flattening both `results.json` files and comparing all 3,076 leaves of `tpp`/`tpp_hindi`/`fertility`/`compression` (0 differences) — so nothing about the existing-practice finding changes; only the causal reading of the T1/T2 numbers does. M1 (monolingual retrain) remains outstanding and is a *harder* test than this one, not the tie-breaker the previous entry anticipated.
Alternatives: reporting the controlled numbers without changing the verdict (rejected — the verdict was explicitly staked on this control in the "Experiment 02 verdict reframed" entry, and leaving "consistent with H2" in place after the control came back negative on prose would be the exact conflation CLAUDE.md §1 forbids); dropping the Itihāsa result as "just meter" (rejected — it is a real controlled flip and should be reported as a verse-specific lead, with its confounds named, rather than suppressed).
Reversible: yes — wording and reporting; the numbers stand on their own in `results.json`.

## 2026-09-03 — English exclusion list: 27,686 hashes; 680 English training sentences dropped
Why: `data/exclusion_hashes_en.txt` (committed) hashes the English side of the same six evaluation splits as the Sanskrit list, in the same source order, with `sentence_hash_en` (sha256 of the stripped text, no transliteration): 27,686 unique hashes from 27,761 sentences, against the Sanskrit list's 27,487 — i.e. English repeats itself less across these splits than Sanskrit does (75 collapsed duplicates vs 274). Building the E1 training corpus then dropped 680 of 118,654 sentences for colliding with it: 675 from Sāmayik train (against 178 on the Sanskrit side) and 5 from Itihāsa train (against 40). The asymmetry is expected and is the same upstream artifact recorded for the Sanskrit side: Sāmayik's English course-material lines ("end-of-lesson questions" and UI strings) collide far more often than their Sanskrit counterparts because several distinct Sanskrit sentences share one English rendering, while Itihāsa's famous repeated ślokas were translated slightly differently at each occurrence, so few of its English lines collide. Final English corpus: `n_out=116,127` after 1,847 exact duplicates removed (`data/processed/manifest_en.json`), against the Sanskrit corpus's 117,720. All four E1 arms reached their requested vocabulary exactly — `E1_bpe_32k` 32,000 (1.6s), `E1_bpe_64k` 64,000 (2.0s), `E1_unigram_32k` 32,000 (3.1s), `E1_unigram_64k` 64,000 (1.1s).
Alternatives: reusing the Sanskrit exclusion list for the English corpus (rejected — it holds SLP1-form hashes of Sanskrit sentences and would match nothing English, i.e. no leakage check at all); skipping the English leakage check because the English side is "only a pivot" (rejected — E1 arms are trained artifacts measured against evaluation splits exactly as T1/T2 are, so CLAUDE.md §2.4 applies to them identically).
Reversible: yes — rebuild with `uv run python experiments/02_tpp_parallel/build_exclusion.py`, which rewrites both lists.

## 2026-09-03 — `hash_fn` (not just `transform`) threaded through the corpus builder; `--retrain` added
Why: three deviations from the Task 6 brief, all small and all forced by the code as it stands. (1) The brief added only a `transform` parameter to `build_training_corpus`; a `hash_fn` parameter was needed too, because that function's own `assert_not_excluded` safety net would otherwise check the English corpus with `sentence_hash`, which transliterates first. Measured while writing the test: the two hash functions in fact *agree* on pure ASCII (`to_slp1` passes Latin through untouched), so the mismatch is not always visible — but any sentence carrying Devanagari (a quoted term in an English gloss) hashes differently under the two, so a mismatched check silently lets that class of leak through. `hash_fn` is therefore threaded through `assert_not_excluded`, `build_exclusion_list`, `build_training_corpus`, `filter_leaked_sentences` and `ensure_training_corpus`, defaulting to `sentence_hash` everywhere (no existing call site changes behaviour). (2) `ensure_training_corpus` gained a `manifest_path` parameter: both corpora live in `data/processed/`, so the English one writes `manifest_en.json` rather than overwriting the Sanskrit `manifest.json`. (3) `train_tokenizers.py` now skips any arm whose `tokenizer.json` already exists, with `--retrain` to force: adding the four E1 arms to `tokenizers.yaml` would otherwise have retrained T1/T2, and a Unigram retrain is not bit-for-bit reproducible (see the earlier entry), which would have silently replaced the artifacts behind numbers already published in `outputs/02_tpp_parallel/results.json`. Verified after the E1 run that all four T1/T2 `tokenizer.json` sha256 values still match the ones the previous `results.json` recorded.
Alternatives for (3): retraining everything each run (rejected — replaces published artifacts and, for Unigram, cannot restore them); a per-arm `skip: true` flag in the config (rejected — encodes a transient fact about the filesystem in a committed config, and goes stale the moment `outputs/` is cleared).
Reversible: yes — all three are additive parameters/flags with the previous behaviour as the default or one flag away.

## 2026-09-03 — `tpp_controlled` is keyed `corpus -> pair -> summary`, not `pair -> variant -> summary`
Why: the Task 6 brief's Interfaces line named `pair -> variant -> summary`, but the task's own binding resolution fixed the variant at `slp1` (T1/T2 have no other) and the figure requires one panel per corpus, so a corpus level is mandatory and a variant level would be a constant. The stored shape is therefore `tpp_controlled[corpus]["<sa_arm>/<en_arm>"] -> summary`, mirroring `tpp`'s corpus-first nesting with the two collapsed levels (variant, pivot) replaced by the pair key that names the pivot. The fixed variant is documented in `run.py`'s `CONTROLLED_VARIANT` and in the README rather than repeated in every entry.
Alternatives: keeping a one-key `variant` level for literal conformance (rejected — a level with exactly one possible value in every entry is noise every consumer has to index past); keying by pair first and corpus second (rejected — every other results block in this experiment is corpus-first, and the figure iterates corpora).
Reversible: yes — re-running the experiment rewrites the block.

## 2026-09-03 — Correction: the "survives against E1" criterion was not a quotation
Why: the entry above ("E1 matched control: the Sāmayik TPP flip does not survive it…") attributes to "the previous entry's own criterion" a quoted sentence — "an in-domain flip that survives against E1 is evidence for H2; one that vanishes is domain fit" — that appears verbatim in no earlier entry. The decision it paraphrases is real and is in "Add a matched English control family E1 for TPP" ("TPP T1/T2 vs E1 becomes the controlled headline; TPP vs o200k/Llama-4 is reported as deployed-practice TPP", together with "whatever gap survives is the language effect H2 is about" as the README put it), but the quotation marks imply a wording that was never written down; the phrasing is in fact the task brief's, not this log's. Recorded here rather than by editing the earlier entry, since this file is append-only (CLAUDE.md §11). Read that sentence as this author's paraphrase of the E1 entry's intent, not as a citation. Nothing about the measurement or the conclusion changes: the criterion, however phrased, is what a matched control is *for*, and the numbers are in `results.json`.
Alternatives: silently rewriting the earlier entry (rejected — append-only, and a log that quietly repairs itself is worth less than one that records its own corrections); leaving it (rejected — a fabricated-looking quotation in the design log is exactly the kind of thing a later reader would try, and fail, to find).
Reversible: n/a — this is a correction, not a design choice.

## 2026-09-03 — Verdict scoped to the raw-subword arms; cross pivots dropped; English exclusion check added
Why: four review fixes, all narrowing claims or narrowing what is computed. (1) **Scope.** The Experiment 02 verdict is now stated as a result about `T1_*`/`T2_*` — raw BPE/Unigram on sandhied, compounded text, which CLAUDE.md §6 lists as baselines — and not as a verdict on H2. The arms this project actually proposes, `T4_*` (sandhi-split, then subword) and `T6_*` (sandhi-split + merges forbidden across gold morpheme boundaries), are Experiments 03 and 04 and are not built; a raw BPE learner has no way to know `tadapi` is `tat + api`, so "matched-size raw subword training does not buy back below-parity TPP on prose" is the strongest claim the evidence supports. The README now names 1.027 on Sāmayik test against `E1_bpe_64k` as the baseline T4/T6 have to beat. (2) **`english_pivots` restricted back to `[T0_o200k, T0_llama4]`.** Listing the four E1 arms there made `results.json["tpp"]` hold every Sanskrit arm x every E1 arm, including combinations nothing interprets (a T0 arm over an in-domain English control is neither deployed practice nor a matched comparison): 6,048 leaves where 2,016 are meaningful. The controlled comparison now comes only from `controlled_pairs`, and `run.py` unions those arm names into `all_arm_names` so the E1 arms still load. Run time falls from 3m58s to 2m22s as a side effect. (3) **`exclusion_check_en`** mirrors `exclusion_check` for the English side of every evaluated corpus, hashed with `sentence_hash_en` against `data/exclusion_hashes_en.txt` (`exclusion_path_en` in the config): the English list is what kept the E1 arms away from this evaluation text, and until now nothing re-verified it at evaluation time. Measured: `n_missing` 0 for all four corpora (2417 + 4047 + 11721 + 1012). (4) The figure suptitle no longer names o200k, since the two columns are measured against different English sides; each column carries its own header naming its pivot.
Alternatives for (2): keeping the cross-entries as "free data" (rejected — every uninterpreted number in a results file is a number someone will eventually quote, and CLAUDE.md §2.5's existing-practice/controlled distinction is exactly what the cross-entries blur).
Reversible: yes — all four are config or wording; re-adding the E1 arms to `english_pivots` restores the cross-entries.
